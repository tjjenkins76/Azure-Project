Connect to Jumpbox VM with Docker from local pc
  
- $ ssh sysadmin@13.77.163.146

To list and verify that the container is on, run the command:

- $ sudo docker container list -a

To start the docker, run the command:

- $ sudo docker start ndly_clarke

To attach the docker, run the command:

- $ sudo docker attatch ndly_clarke
